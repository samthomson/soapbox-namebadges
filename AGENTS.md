# Rules

- **Never commit or push.** The user handles all git commits and pushes themselves.
- Keep this repo focused on badge generation (`generate_badge_2026_oslo.py`, conference variants, and related inputs/docs).
- Avoid reintroducing old treasure-codes modules unless explicitly requested.
- If you change script names, CLI flags, inputs, or outputs, update `README.md` in the same change so docs remain accurate.
